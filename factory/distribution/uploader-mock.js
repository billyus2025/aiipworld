export async function uploadToTikTok(videoPath) {
    console.log(`[MOCK] Uploading ${videoPath} to TikTok... SUCCESS`);
}

export async function uploadToYouTube(videoPath) {
    console.log(`[MOCK] Uploading ${videoPath} to YouTube... SUCCESS`);
}

export async function uploadToInstagram(videoPath) {
    console.log(`[MOCK] Uploading ${videoPath} to Instagram... SUCCESS`);
}
